For an array of n positive integers and an integer k, a subarray is considered good
if it contsists of at least 3 distinct integers.
Find the minimum length of a good subarray. If there is non, return -1

Example:
arr = [2,2,1,1,3]
k = 3

Ans: 4 ([2,1,1,3])
